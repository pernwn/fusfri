import { ButtonGroup } from "@mui/material";

export default function Nav() {
    return (
        <header>
            <nav>
                <ButtonGroup>
                    <FilledBtn name="Button Test" page="/" />
                </ButtonGroup>
            </nav>
        </header>
    )

}